# Physics-Gen Weekly — 2026-W29

> Pulsar 週度前瞻偵察 · 2026-07-10 – 2026-07-17 · 彙整 6 份日報的 ⚡/🔧 · qwen3.5-plus 綜合
> 源檔：2026-07-10, 2026-07-13, 2026-07-14, 2026-07-15, 2026-07-16, 2026-07-17

---

## 本週主軸  
1. **硬約束注入（hard-constraint）從邊緣走向核心**：`Flatness-Preserving Residual Learning`（微分平坦性）、`Quantum Port-Hamiltonian NNs`（辛結構硬編碼）、`Structure-preserving variational neural fields`（守恆律流形嵌入）、`Learning Forced Multibody Dynamics on Lie Groups`（李群上嚴格幾何不變量）——四篇論文共同標誌物理約束已不再僅是損失項或引導梯度，而是直接重構模型架構的**第一性原理**。  
2. **可控性正交化：從「條件輸入」升級為「控制軸設計」**：`Reward Transport`（noise-space alignment 實現 scalar-knob 控制）、`FlowWAM`（光流作為原生可微動作表徵）、`World Narrative Model`（4D物理敘事作為世界狀態接口）——可控性不再依賴 prompt 或 conditioning token，而被建模為**與 latent space 同構、可微分、反事實可驗證的獨立控制軸**。  
3. **rollout 穩定性問題被重構為「物理一致性驗證」問題**：`Cycle-World`（反向預測循環一致）、`RoboWorld`（Step Forcing 閉環動力學錨定）、`SIFT`（self-imagination sim-in-loop 自演化）、`Autonomous Video Generation with Counterfactual Controllability`（embodiment constraints 作為閉環驗證信號）——長程漂移不再靠更強模型，而是靠**生成→驗證→蒸餾的物理閉環**。

## 意外信號  
- **Diffusion 被系統性質疑其因果基礎**：`The Seriality Gap in Video Diffusion Models` 不僅指出 denoising loop 缺乏串行計算能力，更從計算複雜度角度證明其**本質無法支持物理因果鏈推演**——這不是工程缺陷，而是範式級警示，將直接衝擊 diffusion-physics 主流路線的理論合法性。  
- **PBR 不再是渲染後處理，而是可控生成的物理代理（proxy）**：`LightCrafter` 在 cs.CV 和 cs.GR 雙領域均被標為 ⚡，且明確定位為 *structured physical prior* ——這標誌著**物理先驗首次脫離 simulation-in-loop 或 PINN 架構，以輕量、可微、非參數化方式直接驅動視頻級別的可控生成**，打破「物理注入必重」的共識。  
- **LLM 引導的物理注入出現可信度斷層**：`PhyMAGIC`（LLM-guided confidence feedback loop）雖屬 ⚡，但所有 Engineering 評級論文均未提及該技術；相反，`A Self-Evolving Agentic Framework for Metasurface Inverse Design` 則明確將 LLM 替換為**可解釋、固定、可微分的 physics-in-the-loop 模塊**——顯示社區正從「LLM 作為物理推理黑箱」快速轉向「LLM 作為結構化代理協調器，物理由可驗證模塊承擔」。

## 五軸熱度  
| 軸 | 偏向 | 說明 |  
|---|---|---|  
| **output** | `pixel-video` ≫ `latent-video` > `neural-surrogate` | 視頻生成仍佔絕對主導（LightCrafter, WanToFight, World Narrative Model, Motubrain），但 `latent-video`（如 PhyMAGIC, Cycle-World）與 `neural-surrogate`（PGD-NO, Entropy-Constrained Kinetics）在物理保真關鍵場景中穩步上升。 |  
| **injection** | **USP 軸大幅右移：data-only → hard-constraint** | 本週 `hard-constraint` 注入論文達 **11 篇**（含 Quantum PHNNs, Flatness-Preserving, Structure-preserving VNF, Lie-group MB dynamics, Symplectic parachute, Energy-based tensegrity, MUSA-PINN, LOD-MSNO, CaLiSym, Learning Physics-Guided Residual Dynamics, Learning Forced Multibody Dynamics），遠超 `architecture-bias`（9 篇）與 `guidance-gradient`（7 篇）；`data-only` 僅存於 Engineering 層級，Load-bearing 中已完全消失。 |  
| **control** | `scalar-knob` ≈ `action-conditioning` > `geometry` | `scalar-knob`（Reward Transport, MolMiner, BattVAE-GP）與 `action-conditioning`（Motubrain, RoboWorld, SplatCtrl）並列最熱；`geometry`（PanoWorld, UniPhysGen）次之；傳統 `illumination`/`disparity` 等低維控制退居 Engineering 層級。 |  
| **temporal** | `long-horizon-rollout` ≫ `long-form` > `autoregressive-streaming` | `long-horizon-rollout`（含 minute-long, multi-robot, deformable long-term）成核心瓶頸與突破點；`long-form`（LightCrafter）聚焦單一物理過程穩定性；`autoregressive-streaming` 僅存於 ARDY 等 Engineering 工程優化。 |  
| **domain** | `robotics` > `fluid/mechanics` ≫ `molecular/optics` | `robotics`（含 manipulation, locomotion, aerial, surgical）佔 Load-bearing 52%；`fluid/mechanics`（CFD, solid, plasma, geothermal）佔 33%；`molecular/optics/photonics` 合計僅 15%，且多集中於 flow matching / inverse design 子類。 |

## 可證偽觀察清單  
1. **下週若 `hard-constraint` 注入論文數 ≤ 5 篇，則「硬約束主流化」判斷失效**：當前週均 11 篇（7/10–7/17），若下一週顯著回落（如因 `Seriality Gap` 批判導致架構重構延遲），即證偽本週主軸一。  
2. **若 `LightCrafter` 的 PBR-proxy 方法在 Engineering 評級中出現 ≥ 3 篇跟進論文（標註 `injection:architecture-bias · control:illumination`），則「PBR proxy 成為新標準物理代理」成立**：目前僅 LightCrafter 自身被雙領域標 ⚡，尚未見工程級複製；若下週湧現，即驗證其範式擴散力。  
3. **若 `The Seriality Gap` 論文被引用於 ≥ 2 篇新 ⚡ 論文的方法論批判部分（而非僅相關工作），則「diffusion 因果基礎危機」升級為共識**：當前僅該文自身提出，尚未見其他 Load-bearing 工作援引其結論作為設計前提。  
4. **若 `Reward Transport` 的 noise-space alignment 在分子生成之外，出現在 ≥ 1 篇非化學/生物 domain（如 fluid, robotics, optics）的 ⚡ 論文中，則「scalar-knob 控制軸通用化」成立**：當前僅 molecular domain，若下週見於 CFD 或 robot control，即證偽「控制軸仍域綁定」的隱含假設。

---

*Generated 2026-07-17T05:27:31 · `scripts/pulsar/run_weekly.py` · 前瞻偵察（非回顧索引）*
