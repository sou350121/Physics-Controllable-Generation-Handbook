# Physics-Gen Weekly — 2026-W33

> Pulsar 週度前瞻偵察 · 2026-08-07 – 2026-08-14 · 彙整 2 份日報的 ⚡/🔧 · qwen3.5-plus 綜合
> 源檔：2026-08-07, 2026-08-14

---

## 本週主軸  
1. **Physics-as-causal-constraint 正式取代 physics-as-feature-injection**：從 `Overcoming Statistical Bias`（counterfactual consistency）、`How Can Driving World Models Do Counterfactual Prediction?`（abduction-action-prediction）到 `WorldMark` 的 protocol-level action dynamics 評估，因果不變性（而非統計相關性）成為可控生成的**可驗證基準**，標誌著 injection 從 soft bias 向 hard constraint 的範式遷移完成。  
2. **Rollout-free physics conditioning 成為主流技術路徑**：`RIFT`、`ForeWAM`、`MobileWAM`（foresight via gradient only）、`Keep the Future, Drop the Rollout` 等論文共同指向一個明確趨勢——**物理動力學不再需要迭代 rollout 實現**，而是壓縮為 anticipation tokens / future-KV / latent chunk chain，以 architecture-bias 或 guidance-gradient 方式注入，解決 latency-fidelity-controllability 三難困境。  
3. **跨域物理守恆律的神經代理泛化能力爆發**：`MarsCast`（地球→火星）、`EddyFlow`（渦旋譜約束 zero-shot 跨洋盆）、`Quantum Port-Hamiltonian NNs`（量子耗散構造性建模）、`Geometry-aware Incremental Neural Operator`（長程 PDE rollout 的譜穩定性）——**守恆律（能量、動量、譜比、對稱性）正從 domain-specific prior 升級為可遷移、可壓縮、可重組的通用 physics token**。

## 意外信號  
- **「物理注入」與「數據品質」的權衡被系統性打破**：`Seeking Physics in Diffusion Noise`（免微調中間特徵解碼）、`Optimization-Guided Diffusion`（training-free Nash equilibrium loop）、`WorldCycle`（無標註自驗證）表明：**高保真物理一致性不再依賴高質量仿真數據或複雜損失設計，而可從預訓練模型內部結構或推理時優化中直接提取**——這反駁了「physics injection = data + architecture + loss」的傳統共識。  
- **Camera trajectory 首次成為 first-class physics condition**：`SCoPE` 將 sightline coordinate 直接內建於 DiT positional encoding，使 camera motion 從後處理參數升格為生成過程的幾何坐標系錨點——**視角控制不再是 pose-conditioning 的工程技巧，而是物理世界建模的基礎坐標框架**，暗示 video generation 正從「像素序列」向「射線空間演化」轉型。  
- **Robotics 不再是 physics-controllable generation 的終端應用，而是核心 testbed**：`PBD-AG`（支撐關係驅動圖結構）、`D3D-GEN`（domain-grounded RAG）、`Robotic Manipulation is Vision-to-Geometry Mapping`（3D world model as backbone）等論文顯示：**機器人任務的物理約束（剛體穩定性、可見性幾何、交互語義）已成為最嚴苛、最結構化的 physics-conditioning 壓力測試場，其建模需求正在反向定義生成架構的底層設計原則**。

## 五軸熱度  
| 軸 | 偏向 | 說明 |  
|----|------|------|  
| **output** | `latent-surrogate` ≫ `pixel-video` | `MarsCast`, `EddyFlow`, `Quantum PHNN`, `4D-WAM`, `Geometry-aware NO` 均以 latent dynamics / surrogate state 為首要輸出，pixel-level 生成（如 `muSync-GS`）已退居為驗證接口，非建模核心。 |  
| **injection** | **→ hard-constraint**（顯著右移） | `Overcoming Statistical Bias`（consistency constraint）、`WorldCycle`（time-reversibility as hard prior）、`Continual-Learning PINN`（parameter-space hard constraint）、`Quantum PHNN`（構造性耗散）共同推動 injection 光譜從 `data-only` → `aux-loss` → `guidance-gradient` → **`hard-constraint` 成為本週最密集出現的 injection type（7 篇）**，且全部具備可驗證性（causal invariance / symmetry / spectral ratio）。 |  
| **control** | `counterfactual` > `action-dynamics` > `future-conditioning` | counterfactual 已從概念走向量化（ARC/DE metrics）、benchmark（Mini-SSMB）、訓練機制（CoCo），成為 control 軸的絕對主導；`future-conditioning`（RIFT/ForeWAM）與 `action-dynamics`（WorldMark）緊隨其後，形成三足鼎立。 |  
| **temporal** | `long-horizon-rollout` ≈ `rollout-free` | `Multimodal Atmospheric DA`, `SJEPA`, `Geometry-aware NO` 強化 long-horizon fidelity；`RIFT`, `ForeWAM`, `MobileWAM` 則以 rollout-free 架構實現同等物理一致性——二者並非對立，而是同一目標（stable physics evolution）的兩種技術路徑。 |  
| **domain** | `planetary-atmosphere` > `robotics` > `quantum-dynamics` | `MarsCast` 引爆 planetary surrogate 子域；`PBD-AG`, `D3D-GEN`, `4D-WAM` 將 robotics 推至 domain 頂部；`Quantum PHNN` 則開闢首個可控生成意義上的 quantum-dynamics domain，三者均具備明確的守恆律遷移驗證。 |

## 可證偽觀察清單  
1. **下週若出現 ≥2 篇將 `counterfactual consistency` 作為 training objective 的論文（非僅 metric/benchmark），則「causal constraint as optimization target」已成共識**；若僅 0–1 篇，則仍屬前沿探索。  
2. **`RIFT`/`ForeWAM` 類 rollout-free 架構若在 2026-08-21 前被至少 3 篇 🔧 論文採用為 backbone（如用於 robot policy 或 weather downscaling），則證明該範式已進入工程擴散階段；否則仍限於 load-bearing 原型驗證。**  
3. **`MarsCast` 的跨行星遷移若在下週出現 follow-up（如 VenusCast / TitanCast），則 planetary-surrogate 已確立為獨立子域；若無，則仍屬單點突破。**  
4. **`SCoPE` 的 sightline-coordinate PE 若被 `UniWorld-View` 或 `MetaView` 等 3D-aware gen 論文引用並實驗驗證其對 large-baseline view synthesis 的提升（ΔPSNR ≥ 0.8 dB），則 camera geometry 作為 physics-conditioning 基礎框架的地位確立；否則仍屬 niche innovation。**

---

*Generated 2026-08-14T04:23:43 · `scripts/pulsar/run_weekly.py` · 前瞻偵察（非回顧索引）*
